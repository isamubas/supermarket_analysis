pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
xlrd>=2.0

# annex_chinese_fresh_produce_data_analysis
# pyarrow is optional -- it caches the joined frame to parquet. Without it the
# analysis still runs, just rebuilding the join each time.
pyarrow>=14.0
nbformat>=5.9
nbconvert>=7.0
ipykernel>=6.0
